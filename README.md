# `bubbler`

`bubbler` is a theory exploration tool which helps you synthesize (1) conditional rewrite rules, and (2) the _analyses_ which allow your rewrite rules to use them!

stay tuned. this is the future.
